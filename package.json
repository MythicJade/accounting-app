{
  "name": "accounting-app",
  "version": "2.0.0",
  "description": "个人离线记账 PWA 应用",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
